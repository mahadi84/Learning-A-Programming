


✅ CASE–A: How to upload .NET single project into github.com?
✅ CASE–B: How to upload many projects in one link(mono-repo)?
✅ CASE–C: How to ADD/UPDATE/DELETE Project into/form mono-repo?

✅ একই project-এ একাধিক কম্পিউটার থেকে কাজ করার জন্য।




##################################################################################################
          ✅ CASE–A: লোকাল PC-তে আগে থেকেই .NET প্রজেক্ট আছে 
##################################################################################################


git init // এই folder-টাকে Git repository বানায় (tracking শুরু করে)

.gitignore ফাইলে এইগুলো রাখো: // যেসব ফাইল/ফোল্ডার GitHub-এ যাবে না, সেগুলা বাদ দেওয়ার তালিকা
bin/
obj/
.vs/
.user
.suo
appsettings.Development.json

git add .  //# সব নতুন ও পরিবর্তিত ফাইল staging-এ নেয় (commit-এর জন্য প্রস্তুত করে)
git commit -m "Initial commit of .NET project"  // প্রথম snapshot নেয় এবং একটা message দিয়ে ইতিহাসে রাখে
git remote add origin https://github.com/mahadi84/MyDotNetWebAppName  // local project-এর সাথে GitHub repository link করে
git branch -M main  // বর্তমান branch-এর নাম main করে (default/master থেকে main এ)
git push -u origin main // প্রথমবার GitHub-এ কোড পাঠায় এবং upstream সেট করে




### 🔹 Step-1: GitHub এ নতুন repo তৈরি করো
1. GitHub → New Repository
2. Repo name দাও (যেমন: `Login_and_CRUD`)
3. ❌ README / .gitignore / License কিছুই auto-create করো না
4. Create Repository

⚠️ এটা খুব গুরুত্বপূর্ণ—এতে history conflict হবে না।



##################################################################################################
              ✅ CASE–B: একাধিক .NET project একটি GitHub repository-তে রাখতে(যাকে monorepo approach বলা হয়।)
##################################################################################################

## 🎯 তুমি যা চাও: 👉 একটাই GitHub link, কিন্তু ভেতরে একাধিক project। এটাই Monorepo approach।

https://github.com/mahadi84/Dotnet
 ├── Project1
 ├── Project2
 ├── Project3


SAME COMMAND AS:  ✅ CASE–A:


## ❌ যেটা করা উচিত না (Common mistake)
 প্রতিটা project এ আলাদা `git init`
 এক repo এর ভিতরে আরেক repo (nested git)
 Force push দিয়ে history নষ্ট করা



#####################################################################################
          ✅ CASE–C: বিদ্যমান .NET monorepo-তে নতুন project ADD/UPDATE/DELETE করতে 
#####################################################################################

Add(CMD) New Project into the mono-repo:
------------
git add New_Project_Folder_Name
git commit -m "Add New_Project_Folder_Name project files"
git push

Update Existing Project into mono-repo:
------------
git status                  //Git কোন কোন ফাইল বদলেছে দেখো
git add ProjectFolderName  //পরিবর্তনগুলো add করো
git commit -m "Update project logic and fixes"  //কি update করছো লিখে রাখো 
git push                   //GitHub-এ পাঠাও

***Use ESC → :wq → Enter to finish


Delete(CMD) Any Project:
------------
git add -u
git commit -m "Remove New_Project_Folder_Name folder"
git push


#######################################################################################
        🖥️ একই project-এ একাধিক কম্পিউটার থেকে কাজ করার জন্য।
#######################################################################################

## 🖥️ নতুন কম্পিউটার থেকে শুরু করলে কী করবে?

### 1️⃣ প্রথমবার শুধু clone
git clone https://github.com/mahadi84/YourRepoName.git
cd YourRepoName

এতে: পুরো project নামবে * history সহ * কোনো conflict হবে না

### 🟢 কাজ শুরু করার আগে
git pull   // অন্য কম্পিউটার থেকে করা update নামবে

### 🟢 কোড পরিবর্তন করো
### 🟢 কাজ শেষ হলে
git add .
git commit -m "Meaningful update message"
git push




.



.
