
# রিয়েল-ওয়ার্ল্ড সমস্যা সমাধানে ডেটা স্ট্রাকচার 

## ১. প্রশ্ন: কিভাবে একটি হাসপাতালে রোগী ম্যানেজমেন্ট সিস্টেম ডিজাইন করবো?

### সমাধান:
```mermaid
graph TD
    A[নতুন রোগী রেজিস্ট্রেশন] --> B[প্রিয়োরিটি কিউ]
    B --> C{ইমার্জেন্সি লেভেল}
    C -->|হাই| D[তাত্ক্ষণিক চিকিৎসা]
    C -->|লো| E[সাধারণ লাইনে যোগ]
```

ব্যবহৃত ডেটা স্ট্রাকচার:
- প্রিয়োরিটি কিউ: জরুরি রোগীদের অগ্রাধিকার দিতে
- হ্যাশ টেবিল: রোগী আইডি দিয়ে দ্রুত তথ্য খুঁজে পেতে
- লিঙ্কড লিস্ট: ওয়েটিং লিস্ট ম্যানেজমেন্টে

কোড স্নিপেট (Java):
```java
PriorityQueue<Patient> emergencyQueue = new PriorityQueue<>(Comparator.comparingInt(Patient::getEmergencyLevel));

class Patient {
    int id;
    String name;
    int emergencyLevel; // 1-5 স্কেল
}
```

## ২. প্রশ্ন: ই-কমার্স সাইটে প্রোডাক্ট সার্চ অপ্টিমাইজ কিভাবে করব?

### সমাধান:
```
ইউজার সার্চ → ["স্মার্টফোন"] → ট্রাই ডাটা স্ট্রাকচার → ["স্মার্টফোন X12", "স্মার্টফোন Y10"...]
```

ব্যবহৃত ডেটা স্ট্রাকচার:
- ট্রাই (Trie): অটো-কমপ্লিট এবং ফাস্ট প্রিফিক্স সার্চের জন্য
- হ্যাশ ম্যাপ: প্রোডাক্ট ID থেকে ডিটেইলস ম্যাপিং

কোড স্নিপেট (Python):
```python
class TrieNode:
    def __init__(self):
        self.children = {}
        self.products = []

def add_product(root, product_name, product_id):
    node = root
    for char in product_name:
        if char not in node.children:
            node.children[char] = TrieNode()
        node = node.children[char]
    node.products.append(product_id)
```

## ৩. প্রশ্ন: সোশ্যাল মিডিয়াতে মিউচুয়াল ফ্রেন্ড কিভাবে খুঁজব?

### সমাধান:
```
আপনার ফ্রেন্ড লিস্ট: [A, B, C]  
ফ্রেন্ড X এর ফ্রেন্ড লিস্ট: [B, C, D]  
মিউচুয়াল ফ্রেন্ড = ইন্টারসেকশন [B, C]
```

ব্যবহৃত ডেটা স্ট্রাকচার:
- হ্যাশ সেট: দ্রুত ইন্টারসেকশন বের করতে
- গ্রাফ: সোশ্যাল কানেকশন রিপ্রেজেন্টেশনের জন্য

কোড স্নিপেট (JavaScript):
```javascript
const yourFriends = new Set(['A', 'B', 'C']);
const xFriends = new Set(['B', 'C', 'D']);

function findMutual(yourFriends, xFriends) {
    return [...yourFriends].filter(friend => xFriends.has(friend));
}
// Output: ['B', 'C']
```

## ৪. প্রশ্ন: GPS অ্যাপে সবচেয়ে ছোট রুট কিভাবে বের করব?

### সমাধান:
```
ডিজক্স্ট্রা অ্যালগরিদম:
1. সব লোকেশনের দূরত্ব = infinity
2. স্টার্ট পয়েন্ট = 0
3. প্রতিবার নিকটতম নোড ভিজিট করুন
4. নেইবারদের দূরত্ব আপডেট করুন
```

ব্যবহৃত ডেটা স্ট্রাকচার:
- মিন-হিপ: নিকটতম নোড সিলেক্ট করতে
- গ্রাফ: রাস্তা ও কানেকশন রিপ্রেজেন্টেশনের জন্য

ভিজুয়ালাইজেশন:
```
A --5-- B --3-- C
 \     / 
  2   1
   \ /
    D

A থেকে C এর সবচেয়ে ছোট রুট: A → D → B → C (মোট দূরত্ব=6)
```

## ৫. প্রশ্ন: অনলাইন শপিংয়ে রিকমেন্ডেশন সিস্টেম কিভাবে কাজ করে?

### সমাধান:
```
ইউজার A এর পছন্দ = [X, Y]  
ইউজার B এর পছন্দ = [X, Z]  
রিকমেন্ড Z ইউজার A কে
```

ব্যবহৃত ডেটা স্ট্রাকচার:
- ম্যাট্রিক্স: ইউজার-আইটেম প্রেফারেন্স স্টোর করতে
- গ্রাফ: আইটেম সিমিলারিটি ক্যালকুলেশনের জন্য

কোড স্নিপেট (C++):
```cpp
unordered_map<int, vector<int>> userPreferences; // userID -> itemIDs
unordered_map<int, vector<int>> itemUsers; // itemID -> userIDs

vector<int> getRecommendations(int userId) {
    // Collaborative filtering logic
}
```

## প্র্যাকটিস প্রশ্নাবলী
1. একটি কল সেন্টারে কল রুটিং সিস্টেম কিভাবে ডিজাইন করবেন? (ব্যবহার করুন: কিউ)
2. ফাইল এক্সপ্লোরারে ফোল্ডার স্ট্রাকচার কিভাবে ইমপ্লিমেন্ট করবেন? (ব্যবহার করুন: ট্রি)
3. রিয়েল-টাইম স্টক মার্কেট ডেটা ভিজুয়ালাইজেশন? (ব্যবহার করুন: স্ট্যাক/অ্যারে)
4. মুভি স্ট্রিমিংয়ে বাফারিং? (ব্যবহার করুন: সার্কুলার কিউ)

> 💡 টিপ: প্রতিটি সমস্যা সমাধানের আগে নিজেকে জিজ্ঞাসা করুন:
> - কোন অপারেশন সবচেয়ে বেশি হবে (সার্চ/ইনসার্ট/ডিলিট)?
> - ডেটা সাইজ কত বড় হবে?
> - কনকারেন্সি দরকার কি না?






-
